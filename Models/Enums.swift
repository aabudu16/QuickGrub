

import Foundation

enum priceData:String{
    case oneDollar = "$"
    case twoDollars = "$$"
    case threeDollars = "$$$"
    case fourDollars = "$$$$"
}

enum Identifiers:String{
    case categoryCell
}

enum Mode{
    case view
    case select
}
